{
  "name": "nianyun-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "node -e \"console.log('lint: skipped (no linter in SLC)')\"",
    "typecheck": "node -e \"console.log('typecheck: skipped (JS SLC)')\"",
    "test": "node scripts/qa_protocol_smoke.mjs && node scripts/qa_user_journey_smoke.mjs"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^7.1.0"
  }
}
