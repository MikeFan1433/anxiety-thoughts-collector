# 念头 — 可点击原型

替代 v0 的本地 POC。对照 PRD 旅程 A/B，数据存在浏览器 `localStorage`。

```bash
cd /Users/mikefan/Projects/anxiety-thoughts-collector
npm install
npm run dev
```

浏览器打开终端里的本地地址（一般是 `http://localhost:5173`）。桌面端会看到 390px 笔记本画框；手机宽度下铺满屏幕。

预置 3 条念头（内耗 / 推进中 / 已有结果）和 2 条过关记录。清站点数据可恢复种子。
